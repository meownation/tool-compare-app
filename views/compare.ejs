<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title><%= a.name %> vs <%= b.name %> – AI Tool Comparison</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <a href="/compare" class="back-button">
    ← Back to Tool Selection
  </a>

  <h1><%= a.name %> vs <%= b.name %></h1>
  <div class="compare">
    <div class="card">
      <h2><%= a.name %></h2>
      <p><%= a.description %></p>
      <ul>
        <% a.features.forEach(f => { %><li><%= f %></li><% }) %>
      </ul>
      <p><strong>Price:</strong> <%= a.pricing %></p>
      <form action="/out/<%= a.name.toLowerCase() %>" method="get" target="_blank">
        <button type="submit">Try <%= a.name %></button>
      </form>
    </div>
    <div class="card">
      <h2><%= b.name %></h2>
      <p><%= b.description %></p>
      <ul>
        <% b.features.forEach(f => { %><li><%= f %></li><% }) %>
      </ul>
      <p><strong>Price:</strong> <%= b.pricing %></p>
      <form action="/out/<%= b.name.toLowerCase() %>" method="get" target="_blank">
        <button type="submit">Try <%= b.name %></button>
      </form>
    </div>
  </div>
</body>
</html>
